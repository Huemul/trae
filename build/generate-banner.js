module.exports = (version, author, contributors) => {
  const banner = `/**
  * Trae, the fetch library!
  *
  * @version: ${version}
  * @authors: ${author} | ${contributors[0]}
  */`

  return banner
}
